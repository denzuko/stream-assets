baseURL       = "https://denzuko.github.io/stream-assets/"
locale        = "en-us"
title         = "stream-assets"
disableKinds  = ["section","taxonomy","term","RSS","sitemap","robotsTXT"]

[outputs]
  home = ["HTML"]
  page = ["HTML"]

[minify]
  minifyOutput = false
