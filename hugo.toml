baseURL       = "https://stream-assets.cdn.dwightaspencer.com/"
locale        = "en-us"
title         = "stream-assets"
disableKinds  = ["taxonomy","term","RSS","sitemap","robotsTXT"]

[outputs]
  home    = ["HTML"]
  page    = ["HTML"]
  section = ["HTML"]

[minify]
  minifyOutput = false
